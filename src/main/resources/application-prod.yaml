spring:
  datasource:
    url: jdbc:postgresql://db.ngoyyelbscqienoxbuuf.supabase.co
    username: postgres
    password: JumQVikIDRf8qB1d
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update  # erzeugt Tabellen automatisch, kann bei produktiv auch "validate" sein
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
