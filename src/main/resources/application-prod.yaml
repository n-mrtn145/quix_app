spring:
  datasource:
    url: jdbc:postgresql://aws-1-eu-north-1.pooler.supabase.com:6543/postgres?user=postgres.jscsxnjpkpsbvtdxihyn&password=Johannes8,12!20
    username: postgres.jscsxnjpkpsbvtdxihyn
    password: Johannes8,12!20
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
  jpa:
    hibernate:
      ddl-auto: validate  # erzeugt Tabellen automatisch, kann bei produktiv auch "validate" sein
    properties:
      temp:
        use_jdbc_metadata_defaults: false
        provider_disables_autocommit: true
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
