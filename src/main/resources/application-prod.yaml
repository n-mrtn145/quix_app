spring:
  datasource:
    url: postgresql://postgres:JumQVikIDRf8qB1d@db.ngoyyelbscqienoxbuuf.supabase.co:5432/postgres
    username: postgres
    password: JumQVikIDRf8qB1d
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update  # erzeugt Tabellen automatisch, kann bei produktiv auch "validate" sein
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
